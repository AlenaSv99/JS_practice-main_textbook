// Перепишите следующий код через описанный подход:

/*
let arr = [1, 2, 3, 4, 5];
let res = arr[1] + arr[2];

console.log(res); // выведет 5
*/

const arr = [1, 2, 3, 4, 5];
const res = arr[1] + arr[2];

console.log(res); // выведет 5