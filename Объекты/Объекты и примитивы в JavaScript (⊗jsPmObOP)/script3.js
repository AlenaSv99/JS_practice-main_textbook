// Дан следующий код:

let test = [1, 2, 3];
console.log(test); // выведет [1, 2, 3]

// Какой тип данных выведется в консоль? Это примитив или объект?

// Ответ: в консоль выведется тип данных 'object'. Это объект