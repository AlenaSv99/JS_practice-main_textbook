// Код должен вывести последний элемент массива:

/*
let arr = [1, 2, 3, 4, 5];
console.log(arr[arr.length]); // выведет undefined
*/

let arr = [1, 2, 3, 4, 5];
console.log(arr[arr.length - 1]); //выведет 5